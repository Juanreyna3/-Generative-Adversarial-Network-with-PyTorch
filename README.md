# -Generative-Adversarial-Network-with-PyTorch

- created a generator that learned to generate images that look real and a discriminator to distinguish real images from fakes 
- loaded MNIST handwritten dataset and loaded them into batches
- created a discriminator network 
- created a generator network 
- created a loss function and load optimizers 
- trained Generative Adversarial Network (GAN)
